; Always resolve against the public registry for this project, regardless of
; any corporate registry set in ~/.npmrc. A lockfile pointing at an internal
; Artifactory cannot be installed by GitHub's runners.
registry=https://registry.npmjs.org/
